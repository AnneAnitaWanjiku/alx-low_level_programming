#include "main.h"

/**
 *puts2 - prints every char of a string
 *@str: the string to be checked
 */
void puts2(char *str)
{
	int i, int j;
	int j = 0;


	while  (; str[i] != '\0')
	{
		j++;
	}
	if (i = 0; i <= j; i += 2)
	{
		_putchar(str[i]);
	}
		_putchar('\n');
}
